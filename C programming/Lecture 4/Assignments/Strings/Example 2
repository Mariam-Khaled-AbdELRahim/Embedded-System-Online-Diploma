#include <stdio.h>
#include<string.h>
int main()
{
	char string[1000];
	int length=0;
	printf("Enter a string: ");
	gets_s(string);
	while (string[length] != '\0')
	{
		length++;
	}
	printf("Length of string: %d", length);
	return 0;
}
