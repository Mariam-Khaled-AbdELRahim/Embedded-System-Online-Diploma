#include <stdio.h>
#include<string.h>
int main()
{
	char string[1000];
	printf("Enter the string: ");
	gets_s(string);
	printf("Reverse string is: ");
	for (int i = strlen(string); i >= 0; i--)
	{
		printf("%c", string[i]);
	}
	return 0;
}
