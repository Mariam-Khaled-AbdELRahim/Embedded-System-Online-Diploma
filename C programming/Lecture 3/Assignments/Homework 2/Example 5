#include <stdio.h>

int main()
{
  char a;
  printf("Enter a character: ");
  scanf("%c",&a);
  if((a>='a'&& a<='z')||(a>='A'&& a<='Z'))
  {
      printf("\n%c is an alphabet",a);
  }
  else
  {
      printf("\n%c is not an alphabet",a);
  }
}
